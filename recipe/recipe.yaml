context:
  version: "0.1.4"
  python_min: '3.11'

package:
  name: nanonis-xarray
  version: ${{ version }}

source:
  - url: https://pypi.org/packages/source/n/nanonis-xarray/nanonis_xarray-${{ version }}.tar.gz
    sha256: 879343f2cadf797ae2b225c82c39db3faab663ef4b425ffe173b852aed3dbd7e

build:
  number: 0
  script: ${{ PYTHON }} -m pip install .
  noarch: python

requirements:
  host:
    - python ${{ python_min }}.*
    - hatchling
    - hatch-vcs
    - pip
  run:
    - python >=${{ python_min }}
    - numpy >=1.26
    - pint-xarray >=0.4
    - xarray >=2023.9.0

tests:
  - python:
      python_version: ${{ python_min }}.*
      imports:
        - nanonis_xarray
      pip_check: true

about:
  homepage: https://github.com/angelo-peronio/nanonis-xarray
  summary: Read a Nanonis spectroscopy .dat file into a xarray Dataset
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - carlodri
    - angelo-peronio
